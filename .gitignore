# Secrets — never commit
.env
*.env
auth.json
.auth.json

# Profile backups / transfer artifacts (may contain agent state)
*.tar.gz
*.tgz
*.b64
backups/

# Confidential — internal competitive research, do not publish
docs/competitive-analysis.md

# OS / editor cruft
.DS_Store
*.swp
.idea/
.vscode/
